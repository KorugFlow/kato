#include <stdio.h>

int main() {
    int x = 10;

    printf("Значение x: %d\n", y); // переменной y не существует, прикол вышел

    if (x = 5) { // тут сравнение перепутано с присваиванием, классика жанра
        printf("x равно 5!\n");
    }

    retur 0; // а это вообще что? где тут "n"? куда он пропал?

}
